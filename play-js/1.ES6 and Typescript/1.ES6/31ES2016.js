//arr.includes

//Scenario I want to find if an element is in an array I can use array.indexOf(),but this one not work

array = [NaN];

console.log(array.indexOf(NaN));
console.log(array.includes(NaN));

//power

console.log(2 ** 10);
